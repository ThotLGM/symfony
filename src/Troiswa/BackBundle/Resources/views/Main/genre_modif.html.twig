{% extends 'TroiswaBackBundle::layout.html.twig' %}
{% block titre %}
    Modification genre
    {{ parent() }}
{% endblock %}
{% block css %}
    {{ parent() }}
{% endblock %}
{% block central %}
    {% block menuGenres %}class="active"{% endblock %}
    <div id="page-wrapper">

        <div class="container-fluid">

            <!-- Page Heading -->
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">
                        Modification genre
                    </h1>
                    <ol class="breadcrumb">
                        <li>
                            <i class="fa fa-dashboard"></i>  <a href="{{ path('troiswa_back_admin') }}">Dashboard</a>
                        </li>
                        <li>
                            <i class="fa fa-tags"></i>  <a href="{{ path('troiswa_back_genres') }}">Genres</a>
                        </li>
                        <li class="active">
                            <i class="fa fa-user-facetime-video"></i> Modification genre
                        </li>
                    </ol>
                </div>
            </div>
            <!-- /.row -->

            <div class="row">
                <div class="col-lg-6">
                    <div class="table-responsive">
                        {{ form_start(formGenre) }}
                            <div class="form-group">
                                {{ form_label(formGenre.type, 'Type') }}
                                {{ form_widget(formGenre.type, {'attr':{'class':'form-control'}}) }}
                                {{ form_errors(formGenre.type) }}
                            </div>
                            <div class="form-group">
                                {{ form_label(formGenre.description, 'Description', {'label_attr':{'class':'test'}}) }}
                                {{ form_widget(formGenre.description, {'attr':{'class':'form-control'}}) }}
                                {{ form_errors(formGenre.description) }}
                            </div>
                            {{ form_widget(formGenre.modifier, {'attr':{'class':'btn btn-default'}}) }}
                        {{ form_end(formGenre) }}
                    </div>
                </div>
            </div>
            <!-- /.row -->


        </div>
        <!-- /.container-fluid -->

    </div>
    <!-- /#page-wrapper -->
{% endblock %}

{% block javascript %}
    {{ parent() }}
{% endblock %}